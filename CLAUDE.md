# הקשר לפרויקט — קרא לפני עריכה

אתר ליווי סטטי ליחידות לימוד מונגשות. עברית, RTL, ללא בילד ו־ללא תלויות: קובצי HTML שנכתבים על ידי סקריפט פייתון, ומוגשים כמו שהם ב־GitHub Pages.

> 🧪 ממעבדת הניסויים של מרי גרבי

## מבנה

```
index.html                    דף הבית — רשימת היחידות
.nojekyll                     מונע מ-GitHub Pages להריץ Jekyll (חובה)
assets/style.css              כל העיצוב של האתר. קובץ אחד.
build/site.py                 בונה את כל דפי ה-HTML. מקור האמת לתוכן.
units/eretz-israel/
  index.html                  עמוד היחידה
  lesson-1..5.html            עמוד לכל שיעור
  teacher.html                מדריך ההתאמות (מומר מ-Markdown)
  appendix.html               נספח מערכי השיעור המלאים (מומר מ-Markdown)
  sources.html                המערכים המקוריים והערות זכויות
  decks/lesson-1..5.html      המצגות להקרנה. עצמאיות לגמרי — CSS ו-JS מוטמעים.
  files/worksheet-*.pdf       דפי העבודה, קובץ לכל "שביל"
  (המערכים המקוריים אינם מאוחסנים כאן — מקושרים ישירות לאתר משרד החינוך)
```

## כללי עבודה

**אל תערוך ידנית** את `index.html`, `units/*/index.html`, `lesson-*.html`, `teacher.html`, `appendix.html`, `sources.html` — כולם נוצרים מחדש על ידי `build/site.py` וכל עריכה ידנית תימחק. ערוך את הסקריפט והרץ `python3 build/site.py`.

**המצגות** ב־`decks/` הן היוצא מן הכלל: הן נבנות בנפרד ואפשר לערוך אותן ישירות. כל מצגת היא קובץ עצמאי אחד.

**מסמכי המורה** נכתבים ב־Markdown ומומרים ל־HTML בזמן הבילד (`marked`). ערוך את קובצי ה־Markdown, לא את ה־HTML.

## מוסכמות שאסור לשבור

- **קוד צבע:** 🔵 כחול `--a` = עמדה א' (העבר, ההיסטוריה) · 🟠 כתום `--b` = עמדה ב' (ההווה). עקבי בכל היחידה — התלמידים לומדים אותו בשיעור 1.
- **שמות השבילים** (`rimon` / `zayit` / `teena`) הם שמות ניטרליים לשלוש רמות התמיכה. **לעולם אל תכתוב רמת קושי בחומר שמגיע לתלמיד** — המפתח נמצא ב־`teacher.html` בלבד.
- **שמות קבצים באנגלית בלבד.** שמות בעברית עובדים אבל מייצרים כתובות מקודדות ובלתי קריאות.
- **נגישות ונייד:** יעד מגע מינימלי 44px, ניגודיות AA, מיקוד מקלדת גלוי, לחיצה תמיד כחלופה לגרירה.
- **זכויות יוצרים:** אין להטמיע מילות שירים מלאות, מפות או טקסטים ספרותיים מוגנים. ציטוט קצר והפניה בלבד — ראה `sources.html`.

## הוספת יחידה חדשה

1. צור `units/<slug>/` עם אותה מבניות תיקיות.
2. ב־`build/site.py`: העתק את בלוק `LESSONS` ליחידה החדשה והכלל אותו בלולאת הבנייה.
3. הוסף כרטיס ליחידה בדף הבית.
4. שמור על אותו `assets/style.css` — לא ליצור עיצוב נפרד ליחידה.

## פרסום

```bash
git add -A && git commit -m "עדכון תוכן" && git push
```

GitHub Pages: Settings → Pages → Deploy from a branch → `main` / `root`.
